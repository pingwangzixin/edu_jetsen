app.controller('loginCtrl',['$scope','$state','$timeout','$http','$location','$interval','templateServer',function($scope,$state,$timeout,$http,$location,$interval,templateServer) {
	/*$scope.loggedIn = false;
	
	$scope.userInfo = {
		username : '',
		password : ''
	};
	$scope.userLoggedMsg = {
		showTips : false,
		errorTip : ''
	};
	
	//登录
	var regExp = /^([G|L][1-9]\d{5}[1-9]\d{3}((0\d)|(1[0-2]))(([0|1|2]\d)|3[0-1])\d{3}([0-9]|X))|((13[0-9]|14[0-9]|15[0-9]|17[0-9]|18[0-9])\d{8})$/;
	$scope.loginT = function (){
		if(! regExp.test($scope.userInfo.username) || $scope.userInfo.username == '' || $scope.userInfo.password == ''){
			$scope.userLoggedMsg.showTips = true;
			$scope.userLoggedMsg.errorTip = '用户名或密码错误';
		}else{
			$http.get(requireIp + 'jeuc/api/uc/login?username='+$scope.userInfo.username+"&userType=1&password="+$scope.userInfo.password).success(function(data) {
	    		console.log(data);
	    		if(data.ret==200){
					sessionStorage.setItem('userId',data.data.id);
					$state.go('wrap.index',null,{reload:true})
	    		}else{
	    			$scope.userLoggedMsg.showTips = true;
	    			$scope.userLoggedMsg.errorTip = '用户名或密码错误';
	    		}
		    }).error(function(){
    			$scope.userLoggedMsg.errorTip = '系统错误';
		    });
		}
	}*/
}]);
